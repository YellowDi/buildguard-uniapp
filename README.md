# BuildGuard UniApp

BuildGuard 的 `uni-app` 项目，用于小程序和 `uni-app` H5 构建链路。

## 开发

```bash
pnpm install
pnpm dev:mp-weixin
```

## 构建

```bash
pnpm build:mp-weixin
pnpm build:h5
```

## 说明

- 当前仓库仅负责 `uni-app` 版本。
